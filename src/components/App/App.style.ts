import styled from "styled-components";

import { theme } from "../../utils/theme";

export const MainContainer = styled.div`
  display: flex;
`;

export const ListWrapper = styled.div`
  width: ${theme.distances.menuWidth}px;
  flex-shrink: 0;
`;
