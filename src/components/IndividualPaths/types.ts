import {
  FocusedIndividualIdType,
  IndividualListType,
} from "../../types/globalTypes";

export type IndividualPathsProps = {
  positionsByIndividual: IndividualListType;
  focusedIndividualId: FocusedIndividualIdType;
};
